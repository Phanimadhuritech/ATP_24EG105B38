//write a function that recives an array and search element as args and returns the index of that search elemennt in the array.it should return "not found" when search element not found .

function Search(a,key)
{
    for(let i=0;i<a.length;i++)
        if(a=key)
            return a
        console.log("not found")
}
console.let(Search(a,key))

